# EULLM Forge — R&D Roadmap: distillation & verticalization

**Baseline:** Forge pipeline v0.x (prune → distill → quant → identity LoRA → GGUF) · July 2026
**Purpose:** the verticalization *pipeline already exists and runs* (see "Current state"). This
roadmap is about (a) the one missing prerequisite — a real **evaluation harness** — and (b)
deciding the distillation **recipe** empirically instead of by gut feeling.

Operational document: every item has a checkbox and a tag —
**[✅ done]** already implemented · **[🔧 fix]** exists but needs work/reconciling ·
**[🆕 new]** to be created. An item is "done" only when it meets the Definition of Done at the bottom.

---

## Current state (honest inventory)

### ✅ Already built (≈10.4 k LOC Python, with tests)
- **Full pipeline orchestrator** — `pipeline.py` runs prune → distill → quant → identity → export from a profile YAML.
- **KL distillation — implemented, real.** Soft-target `KL·T²` + hard-target `CE`, temperature, frozen teacher. Two entry points: the library (`eullm_forge/distill.py`) and a production runner (`scripts/distill.py`, 555 LOC: *Qwen3-32B → Qwen3-7B-Base*, single-GPU ~96 GB, checkpoint-resumable).
- **Quantized teacher already supported** — `--teacher-load-in-4bit` / `--8bit` (bitsandbytes NF4).
- **Pruning / Quantization / Identity LoRA / GGUF export** — all implemented as modules.
- **Italian-legal data pipeline** — Cassazione fetch (`italgiure.py`), **anonymizer** (`anonymize.py`, 700 LOC), dedup, chunking, corpus validation (`datasets/legal_it.py`, 1.9 k LOC + scripts).
- **Training configs** — `continued_pt_qwen3_32b.yaml` (Phase 1), `distill_qwen3_32b_to_7b.yaml` (Phase 2), `smoke_qwen3_1.7b.yaml` (smoke).
- **Ops scripts** — env setup, dependency install, GPU env check, `train.sh`, `quantize_to_gguf.sh`.
- **Tests** — ~1.5 k LOC (`test_anonymize` 620, `test_italgiure`, `test_dedup`, `test_pipeline`, `test_distill`, …).
- **1.7B smoke run: PASS.**

> **In short: arm A (32B + KL) is code-complete and runnable today.** The gap is *evaluation*, not the pipeline.

### 🔧 To fix / reconcile
- **Run the full-scale 32B → 7B distillation.** Only the 1.7B smoke has actually been executed; no production model has been produced yet (Hub has no models).
- **Reconcile two coexisting recipes.** `pipeline.py` prunes a base then self-distills (Minitron-style, profile `base_model: Qwen3-14B`); `scripts/distill.py` + training configs distil a **32B teacher into a fresh 7B-Base** (no pruning). Decide the canonical recipe (prune-then-distil vs big-teacher→fresh-student).
- **Align teacher declarations.** Profiles say `Qwen3-14B`; strategy/configs say `Qwen3-32B`. Pick one and update the profiles + `CLAUDE.md` ("up to 32B teacher") accordingly — e.g. *32B FP8 as KL default, 70B-4bit via SeqKD as the upgrade path*.
- **Promote in-loop perplexity to a real held-out metric** (this is the on-ramp to F0).

### 🆕 To create
- **Evaluation harness (F0)** — legal-QA benchmark + LLM-as-judge + blind A/B. No eval module exists today (only `.eval()` mode + a human spot-check).
- **SeqKD (sequence-level)** — teacher-generated corpus + student SFT. Only KL exists.
- **Hybrid** — KL from a 32B + synthetic data generated by a 70B, mixed in.
- **Forge-generated per-model compliance card** (Annex XI §1 + training summary + copyright + provenance). Not implemented; today the Hub serves a static stub.
- **Membership-inference PII probe** before publishing weights (planned in strategy, not implemented).

---

## Binding principles
- **Apache-2.0 teacher** (or compatible permissive licence). No Llama in the default catalog.
- **KL only with an identical tokenizer** (same family/vocab, Qwen3 ↔ Qwen3) — else logit-level KL is undefined.
- **Dense, not MoE** for KL (routing adds logit variance).
- **Single-GPU 94–96 GB** reference constraint; a frozen teacher is loaded quantized (70B@4-bit ≈ 35 GB, *lighter* than 30B@FP16 ≈ 60 GB).
- **GDPR-safe dataset**: anonymized corpus only, never raw data.
- **Isolate variables**: one change at a time (teacher size / method), same dataset, same student init, same budget, same seed.
- **Eval before models.**

---

## F0 — Evaluation harness *(HARD PREREQUISITE — blocking)* · 🆕
**Exit gate:** a reproducible, blind way to rank two Italian-legal models exists.

- [ ] **F0-A · Italian legal held-out set** — 200–500 questions/prompts with reference answers or a rubric (civil, criminal, GDPR, procedure). Outside the training set. Versioned. **🆕**
- [ ] **F0-B · Metrics** — perplexity on held-out legal text **[🔧 promote from in-loop]**; QA accuracy **🆕**; **blind LLM-as-judge** **🆕**; lawyer spot-check (~30–50 items) **🆕**.
- [ ] **F0-C · Reusability** — parametric by domain/language, reusable for `medical-de` / `finance-fr`. **🆕**

---

## F1 — Comparative pilot *(at REDUCED scale)*
**Exit gate:** an honest directional signal on which recipe wins, at minimum cost.
Same anonymized dataset, same student `Qwen3-7B-Base`, identical budget.

- [ ] **A · 32B + KL** — `Qwen3-32B` (FP8), logit-level KD. **[✅ implemented → just run at scale + measure]**
- [ ] **B · 32B + SeqKD** — same teacher, sequence-level (SFT on generated text). Isolates the **method**. **🆕**
- [ ] **C · 70B-4bit + SeqKD** — isolates teacher **size** (vs B). **🆕**
- [ ] **H · Hybrid** — 32B KL + 70B-generated synthetic data. Often the winner. **🆕**
- [ ] Run at **reduced scale** for the signal, not final models. **🔧**
- [ ] Budget the **synthetic-corpus generation** with the 70B separately (hidden cost of SeqKD/H). **🆕**
- [ ] Record per arm: teacher, precision, method, hyperparameters, GPU hours, cost. **🔧**

**Decision gate F1 → F2:** pick **one** production recipe from the F0 numbers. Other arms remain a documented ablation.

---

## F2 — Scale the winner → production `legal-it-7b`
**Exit gate:** an `eullm/legal-it-7b` Q4_K_M GGUF that beats base Qwen3-7B on the F0 eval, with a Forge-generated compliance card.

- [ ] Full run of the winning recipe on the complete dataset. **[✅ pipeline exists → 🔧 execute at scale]**
- [ ] Optional identity LoRA (branding). **[✅ implemented]**
- [ ] GGUF Q4_K_M export + local smoke-test via the Engine. **[✅ implemented]**
- [ ] **Per-model compliance card generated by Forge** (see `compliance-card-template`). **🆕**
- [ ] Membership-inference PII probe before publishing weights. **🆕**

---

## F3 — Generalization
**Exit gate:** the winning recipe is a *function* of (domain, language), not a one-off.

- [ ] Apply the winning recipe to `medical-de-7b` and `finance-fr-7b` with the same harness. **🆕** *(dataset modules `medical_de.py` / `finance_fr.py` are stubs today — 🔧 flesh out)*
- [ ] Consolidate the recipe into `pipeline.py` defaults and the profiles. **🔧**
- [ ] Re-evaluate 70B-4bit-SeqKD as a v0.2 quality upgrade if F1 points that way. **🆕**

---

## Cross-cutting
- **Compliance is a by-product of the research.** The F1 ablation (design choices + rationale + evaluation) populates the Annex XI fields of the compliance card. The research *is* AI Act documentation.
- **Naming.** Internally `legal-it-7b-{kl,seqkd,hybrid}`; publicly a **single** `eullm/legal-it-7b` (the winner).
- **Budget.** Estimate cost/arm *before* F1; a reduced pilot ≪ two full models.
- **GPAI-provider risk (AI Act).** If a recipe crosses ~1/3 of the base compute, document as a GPAI provider (covered in the compliance dossier).

---

## Definition of Done (per item)
1. Reproducible: single command + versioned config, idempotent resume-from-checkpoint.
2. Measured: numbers on the F0 eval, not impressions.
3. Recorded: teacher / method / hyperparameters / hours / cost in the model changelog.
4. GDPR-safe: anonymized corpus only; no raw data or PII in the weights (membership-inference probe).
